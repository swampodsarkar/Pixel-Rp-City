# Neo Pixel City

Multiplayer 3D open-world game with spatial voice chat — powered entirely by Firebase Realtime DB.

## Run Locally

1. `npm install`
2. `npm run dev`
3. Open http://localhost:3000

No server-side infrastructure needed. Everything runs through Firebase.
